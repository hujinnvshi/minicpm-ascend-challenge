# 工作日志：2026-08-04（910B3 环境部署 + cann 后端攻坚）

## 概述
进入厂家 910B3 云环境（替代 910C；厂家授权，1 颗 910C = 2 颗 910B），完成环境基线对齐 +
ggml-cann 后端 3 轮攻坚（P0 跑通 / P1 诊断 / P1.5 单工 offload）。当日 4 个 commit（main，待 push）。

## 今日工作链

### 1. 环境基线对齐（`da4b86b`）
- 实测 910B3 云环境（CANN 9.1.0-beta.3 / 鲲鹏 256 核 / 2TB 内存 / 64G HBM）
- 发现官方预置全套权重（`shared_assets` 11 档+全模块）+ `code/daily-omni/` benchmark 代码 → 跳过下载/数据准备
- 新增 `docs/env-scan.md`（环境基线）；修订 README/workflow-overview/reproduce-guide 等执行文档（910C→910B3、截止日 8/17→8/31、权重预置、图模式不支持）

### 2. P0 全链路跑通（`aa9ffea`）
- **cann 补丁 1-5**（`ggml-cann.cpp`）：T2W 线程 device 绑定（`set/get_tensor_async` + `event_record/wait`）+ `GGML_OP_SQR` 断言放宽
- 9 用例验证（Q4_K_M）：T2W RTF mean **0.861**，87% chunk 实时，0 静音，LLM 视觉语义正常
- 脚本修复：`build-cann.sh`（补 `-S` 源目录）、`capture-env.sh`（hostname 健壮化）

### 3. P1 LLM offload 诊断（`62d39d2`）
- 诊断 LLM 在 CPU（AICore=0、HBM 3.4G）；探针定位 `host_buffer` cap + 量化算子缺失
- **战略发现**：cann 对 **Q4_K_M 量化不支持**（fallback CPU，prefill 7.9s），对 **F16 支持**（NPU，prefill 0.58s，13x）→ 910B LLM 改用 F16
- F16 perf-duplex **TTS RTF 0.99**（首个与 4090 实验002/016 的 0.75 横向比的有效基线）

### 4. P1.5 host_buffer 修复（`a02f1ed`）
- **cann 补丁 6**：`device_get_props` 的 `host_buffer` 默认 false（env 翻转 `NO_PINNED`→`FORCE_PINNED`）
- **单工成功**：F16 LLM 稳定上 NPU（HBM 23.6G + AICore 66% + prefill 0.77s，不需 env）
- **双工未解**：perf-duplex 双工 LLM 仍 CPU（`duplex_llm_thread_func` 路径问题，下阶段）

## 关键数据（均实测）

| 项 | 值 | 来源 |
|---|---|---|
| cann 补丁总数 | 6 处（`ggml-cann.cpp`） | 代码 |
| F16 perf-duplex TTS RTF | **0.99**（vs 4090 0.75） | perf-duplex 报告 |
| 单工 F16 LLM | HBM **23.6G** + AICore **66%** + prefill **0.77s** | npu-smi + log |
| 双工 LLM | 仍 CPU（HBM 3.4G + AICore 0） | npu-smi |
| Q4_K_M LLM prefill | 7.9s（CPU fallback） vs F16 0.58s（NPU） | omni-cli log |

## 遗留（下阶段）

- **P1.6（最高）**：双工 `duplex_llm_thread_func` LLM 上 NPU——双工评测基线前提
- **P2**：量化重扫（cann 支持档：F16/Q8_0/Q6_K/...；Q8_0 在 cann 可能正常，无 CUDA 乱码 bug）
- **P2.5**：基于 F16 TTS 0.99 基线优化 TTS/Token2Wav 瓶颈段

## 文件改动索引

- **代码**：`code/llama.cpp-omni/ggml/src/ggml-cann/ggml-cann.cpp`（6 补丁）、`scripts/build-cann.sh`、`scripts/capture-env.sh`
- **文档**：`docs/cann-patches.md`（6 补丁 + 已知问题）、`docs/experiments.md`（P0/P1/P1.5）、`docs/decisions.md`（910B3/F16/host_buffer 决策）、`docs/env-scan.md`（新）、`README.md`、`docs/workflow-overview.md`、`docs/reproduce-guide.md` 等

## commit 链（main，待 push）

| commit | 摘要 |
|---|---|
| `da4b86b` | 按 910B3 实测环境修订项目基线 |
| `aa9ffea` | P0 cann 补丁 1-5 + 9 用例验证 |
| `62d39d2` | P1 落盘：cann 量化缺失→F16 战略 + F16 TTS RTF 0.99 基线 |
| `a02f1ed` | P1.5 补丁 6 host_buffer 默认 false——单工 LLM 上 NPU |
