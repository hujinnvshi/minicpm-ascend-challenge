# 会话交接（2026-08-25）— 赛事说明相符性核对 + 提交调度分析

> 今日主题：按用户提供的赛事说明（子赛道 A：llama.cpp-omni 推理优化，CANN 9.1.0-beta1 镜像，
> 核心指标=每 chunk RTF）逐条核对筹备相符性 → 全部对齐，发现 v6 包 README 哈希无效问题 →
> 提交调度状态分析（用户反馈：提交一次都没被调度到评测）。
> 新会话先读本文件 + `docs/session-2026-08-24.md`（上一交接）+ `docs/ops-handoff.md`（提交记录）。

## 一、当前状态（一句话）

**v6（8/24 提交，本机 RTF 中位 1.166）在官方队列；用户反馈提交从未被调度到评测——
榜单（8/24 21:17 刷新）10 队全为 8/11-8/21 旧分，8/21 后官方零新增出分。
提交状态查询需重登（旧 cookie 8/25 已对 commit 接口失效，checkLogin 0000 但 get_all_commit_data 900007）。**

## 二、今日成果（赛事说明逐条核对）

用户提供赛事说明原文（赛道一·子赛道 A）：指定模型 MiniCPM-o 4.5 / 指定框架 llama.cpp-omni /
镜像版本 CANN 9.1.0-beta1 / 优化目标=精度+Demo 可用性前提下降低每 chunk RTF / RTF=chunk 生成耗时÷chunk 时长。

| # | 核对项 | 结论 | 证据 |
|---|---|---|---|
| 1 | 指定模型 MiniCPM-o 4.5 | ✅ | shared_assets/models/OpenBMB/MiniCPM-o-4_5-gguf 全套（F16 主+vision/audio/tts/token2wav） |
| 2 | 指定框架 llama.cpp-omni | ✅ | 官方 bench/huawei b06198f 基线，4 文件改动全 env 门控 |
| 3 | 镜像 CANN 9.1.0-beta1 | ✅ 兼容 | 本机 beta.3 向上兼容（env-scan.md:32 已评估）；官方统一评测=910C，提交物不含环境 |
| 4 | 精度保证 | ✅ | Daily 79.8% / TTS WER 0.97% ASV 0.708 / VideoMME 72.9%（官方采样 0.1，197/270） |
| 5 | Demo 可用性 | ✅ | 3 进程端到端跑通 + 新机复验（demo-verification） |
| 6 | RTF 降低 | ✅ | 本机口径 1.71→1.166：FA(-19.6%)/VPM FA(-2.1%)/NPU 串行(-3.3%)/head 并行(-14.3%) |
| 7 | RTF 口径对齐 | ✅ | 官方=core 帧 pooled Σcompute/Σaudio（非 per-chunk 平均，官方明示"全 chunk 平均会误导"） |
| 8 | 提交物合规 | ✅ | SUBMISSION_GUIDE 四件套；v6 提交 result 0000 |

## 三、发现的问题

### ① 🔴 v6 包 README 最终提交哈希无效（2026-08-25 实测）
- v6 包内 llama.cpp-omni.zip 与 staging HEAD `1dd42b7` 的 git archive **逐字节一致**
  （sha256 d89d91c4a6f83f64 吻合）→ 代码包正确
- 但外层 README 写"最终提交哈希 af67cfe"——af67cfe 在主仓库与 staging 仓库**均不存在**
  （git cat-file 无效对象名）
- 推断：v5 时代（8/22）staging HEAD=af67cfe；8/24 v6 打包时 staging 仓库重建
  （重 clone 官方+覆盖提交 → 1dd42b7），README 哈希未同步
- 风险：SUBMISSION_GUIDE §4.1 硬性要求最终提交哈希，人工复核对照 llama.cpp-omni.zip 内
  git log（HEAD=1dd42b7）会不一致 → 打回风险（v5 曾因"待填"占位被打回一次）
- **修复：v7 重打包 README 哈希改 1dd42b7，或出分后合并修正**

### ② 🟡 提交调度状态（用户反馈"一次都没被调度到资源评审"）
- 榜单 8/21 后零新增分数（10 队全 8/11-8/21 旧分）→ 官方评测输出停滞或队列极长
- v1/v2（8/15-16）、v3/v4（8/21）、v5（8/22）、v6（8/24）均未出分
- 可能原因（按概率）：① 官方队列长（8/21 公告"按顺序依次执行"，910C 资源有限）②
  官方资源暂停/积压 ③ 提交被搁置（低概率，upload_model 均 0000，但"提交成功"≠"进入评测"）
  ④ 榜单刷新滞后
- **唯一区分途径：重登（sendmessage→验证码→login）→ get_all_commit_data state + get_commit_log**
- 行动：群内反馈（8/21 公告明示"如有疑问，可在群内反馈"）；停止无效重复提交

### ③ ⚪ 截止倒计时
- 截止 2026-08-31，剩 6 天；v5/v6 最快 8/26-8/27 出分（2-3 天周期）

## 四、关键坑（今日确认）

1. **checkLogin 0000 也不可靠**：8/25 旧 cookie checkLogin 返回 0000（数据完整），但
   get_all_commit_data 返回 900007 未登录——checkLogin 判定与真实接口权限不一致（与
   skill"checkLogin 0002 不可靠"对称）。**查提交状态以 get_all_commit_data 为准**
2. get_submit_status（提交开放状态）无需登录态即可查（resultcode 0000，race 0 仍开放）
3. 排行榜 get_rank_list 带旧 cookie 可查（公开性高）——榜单数据本身不受登录态影响

## 五、下一步

1. **重登查提交状态**（需用户手机验证码）：sendmessage(userID=18510911437,type=phone,
   country_code=+86) → login → get_all_commit_data + get_commit_log
2. 若确认排队：群内反馈调度进度；等待 v5/v6 出分（8/26-8/27）
3. 出分后对照本机 1.166；偏差大走官方"共同审核"通道
4. 若需 v7：README 哈希改 1dd42b7（修问题①）
