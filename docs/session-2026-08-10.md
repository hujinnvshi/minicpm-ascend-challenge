# 会话交接 2026-08-10（新会话先读本文 + session-2026-08-05.md）

## 今天做了什么（commit 链，已 push origin/fix/video-extract-harden）
```
c0da7b2  docs: P3 CookBook 实证 + 邮件补 910C 确认
ab5653e  feat: 接入 OpenSQZ CookBook 官方 pipeline (videomme/daily-omni)
e1d79f4  fix: build-cann.sh 固化 ccec (系统 gcc 12.3.1 build 不了 llama.cpp)
62e0e01  chore: 接入前基线快照
```

## 四个关键发现
1. **官方评测 pipeline = `OpenSQZ/MiniCPM-V-CookBook` 的 `evaluation/`**（videomme/daily-omni/tts_seed 子目录）。前几轮一直找错地方（在 tc-mb/llama.cpp-omni 找）。评测走 `llama-omni-eval-cli`（pipe 驱动，非 WS server）。
2. **build 解 = ccec（CANN bisheng clang 15）**。系统 gcc 12.3.1（openEuler）生成的 .o 在 COMDAT/binding/symtab 多方面异常，bfd/lld/gold 三 linker 全失败；ccec 干净编过，但要补 `-lstdc++ -lm -lpthread -ldl`。`build-cann.sh` 已固化。
3. **多帧退化 = 910B/CANN 后端框架 bug**（非 context/喂法/编译器）：CookBook 官方 pipeline + 64 帧 + ctx 40960 也退化（0/2，输出 `_`）；vLLM-Omni 同模型多帧正常（模型本身支持）；别人 910B4 也退化（37.5%/36.9%）。**不能确定是硬件**（910B vs 910C），需 910C 对照（见 `docs/new-env-verify.md`）。
4. **官方基线 69.0/79.5 极可能 910C 实测**（推断，待赛方确认）。

## 当前状态
- **binary 全恢复**（ccec build）+ 备份 `/tmp/build-cann-bin.bak`（12M）。perf 验证 RTF 0.49–0.72（中位 0.68），ccec binary 可用、性能不退化。
- **CookBook pipeline 接入**（`benchmark/video-mme-cookbook/`，ccec build eval-cli + .env + 数据）。smoke_test 跑通但多帧退化。
- **赛事满足度**：性能 RTF ✅（0.68<1.087）、Demo ✅、TTS-WER ✅（0.20）、TTS-ASV ⚠️（0.84 base-plus 非官方 s3prl 口径）、VideoMME/Daily-Omni ❌（910B3 多帧退化）。
- **邮件待发**：`docs/organizer-inquiry-final.md`（含 CookBook 实证 + 910C 基线确认请求）。内部 SMTP 封，需用户腾讯企业邮箱客户端手动发。

## 教训（已记 memory `destructive-op-rollback-first`）
破坏性操作（`rm -rf`/clean rebuild）前必须先备份。今天 `rm -rf build-cann` 删了 Aug4 binary（无备份、git 不跟踪），后靠 ccec rebuild 恢复。后续 rm/build 前先 `cp -a` 备份。

## 下一步（优先级）
1. **新环境对照实验**（可能 910C，镜像匹配）→ 确定硬件 vs 软件根因。`docs/new-env-verify.md`。
2. **发邮件**（用户外部动作）：`organizer-inquiry-final.md` → 群内反馈或腾讯企业邮箱。
3. （可选）**ASV 官方口径**（s3prl + `wavlm_large_finetune.pth`）+ **WER 全量**（zh+en）—— 精度从 1/4 → 潜在 3/4 达标。

## 关键命令
```bash
# build（ccec 固化）
bash scripts/build-cann.sh
# 性能验证
cd code/llama.cpp-omni && build-cann/bin/llama-omni-perf-duplex -m .../MiniCPM-o-4_5-F16.gguf -c 4096 -ngl 99 --ref-audio ... --test ... 36 -o ... --out-json ...
# CookBook smoke_test（验证多帧退化）
cd benchmark/video-mme-cookbook && python3 smoke_test.py 2
# NPU 监测
npu-smi info -t usages -i 0
```

## 关键路径
- 代码：`code/llama.cpp-omni/`（tc-mb fork + 6 CANN 补丁 + CookBook eval patch）
- 模型：`/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5-gguf/`（F16 主 + audio/vision/tts/token2wav 子目录）
- 数据：`/workspace/shared_assets/datasets/`（Video-MME / Daily-Omni / seed-tts-eval）
- CANN：`/usr/local/Ascend/cann-9.1.0-beta.3`；ccec：`.../tools/bisheng_compiler/bin/ccec`
- venv：`/workspace/venv-g23`（缺 pandas/PIL/dotenv，已 pip 装；decord 无）
