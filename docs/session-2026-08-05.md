# 会话交接(2026-08-05)— 新会话从这里恢复

> 本文件是会话状态快照 + 恢复指南。新会话先读本文件 + [memory MEMORY.md](../../../.claude/...) + `docs/eval-spec.md` + `docs/performance-report.md`。
> 代码库:`/workspace/user_data/temp_project/minicpm-ascend-challenge`(= 910B 云机,本地无公网入站)。

## 一、当前进度(一句话)

**性能 + Demo + 复现 + 报告全 done(已 push main);卡在等官方 llama.cpp-omni benchmark 评测脚本跑 3 项精度数。** F16 不改推理数学 → 精度预期 = 基线,跑通即过准入。

## 二、已完成(均 push 到 GitHub main `0ff3e83`)

| 项 | 内容 | 产出 |
|---|---|---|
| **P1.7 性能** | LLM↔TTS 队列 1→16 解耦 → **SPEAK→WAV RTF 0.83**(基线 1.087,beat 24%) | `experiments.md` P1.7 + `omni.cpp` 改动(on main) |
| **G3 Demo** | 3 进程栈(gateway+worker+backend)端到端跑通 + turnchat 演示视频 | `benchmark/demo-video/demo_turnchat.webm` |
| **G5 报告** | 性能报告(8 字段)+ 复现说明 + 官方评测规范 | `performance-report.md` / `reproduce-guide.md` / `eval-spec.md` |
| **G2 数据/指标基建** | TTS-Seed testset 本地解压 + WavLM SIM 模型 + torch/transformers/jiwer 装 + vllm-omni eval 脚本拷贝 | `benchmark/seed-tts-eval/{seedtts_testset/(gitignored),eval_ref/}` |
| **cann 补丁** | 6 处 ggml-cann 补丁(device 绑定/host_buffer/SQR) | `cann-patches.md` |

## 三、关键路径(都在这台 910B 机)

- **模型(只读预置)**:`/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5-gguf/` → 主 `MiniCPM-o-4_5-F16.gguf` + vision/audio/tts/projector F16 + token2wav-gguf/。
- **代码**:`code/llama.cpp-omni/`(fork);`code/MiniCPM-o-Demo/`(官方 Demo)。
- **构建产物**:`code/llama.cpp-omni/build-cann/bin/{llama-omni-cli,llama-omni-perf-duplex,llama-omni-server,llama-bench}`。
- **Python venv**:`/workspace/venv-g23`(torch2.13 + transformers + jiwer + zhon + opencv + openai + playwright+chromium + imageio-ffmpeg)。激活:`source /workspace/venv-g23/bin/activate`。**ffmpeg 二进制**:`/workspace/venv-g23/lib/.../imageio_ffmpeg/binaries/ffmpeg-linux-aarch64-v7.0.2`。
- **TTS-Seed 数据**:`benchmark/seed-tts-eval/seedtts_testset/{en,zh}/`(meta.lst + prompt-wavs + wavs/gt,gitignored)+ WavLM `shared_assets/.../CowboyZ/seed-tts-eval/wavlm_large_finetune.pth`。
- **perf 工具**:`code/llama.cpp-omni/tools/omni/perf/{run_perf.sh,analyze_perf.py,perf-duplex.cpp}`;日志 `tools/omni/output/`。
- **NPU 监测**:`npu-smi info -t usages -i 1`(NPU id=1,看 Aicore Usage + HBM Bandwidth Usage)。

## 四、关键命令(恢复即用)

**跑 SPEAK→WAV RTF**:
```bash
cd code/llama.cpp-omni
BIN=build-cann/bin/llama-omni-perf-duplex
MODEL=/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5-gguf/MiniCPM-o-4_5-F16.gguf
PREFIX=$PWD/tools/omni/assets/test_case/duplex_omni_test_case/duplex_omni_test_case_
REF=$PWD/tools/omni/assets/default_ref_audio/default_ref_audio.wav
$BIN -m "$MODEL" -c 4096 -ngl 99 --ref-audio "$REF" --test "$PREFIX" 36 -o tools/omni/output --out-json tools/omni/output/perf.json
python3 tools/omni/perf/analyze_perf.py tools/omni/output/perf.json --interval-ms 1000
```
**起 Demo 3 进程**(详见 `docs/reproduce-guide.md` §6):
```bash
cd code/MiniCPM-o-Demo
VENV=/workspace/venv-g23/bin/python
SERVER=../llama.cpp-omni/build-cann/bin/llama-omni-server
MODEL=/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5-gguf/MiniCPM-o-4_5-F16.gguf
$VENV gateway.py --port 8006 --internal-port 8007 --host 0.0.0.0 --https --ssl-certfile certs/cert.pem --ssl-keyfile certs/key.pem &
$SERVER -m "$MODEL" -ngl 99 --host 127.0.0.1 --port 22500 -c 8192 --no-mmap &
$VENV worker.py --host 0.0.0.0 --port 22400 --gpu-id 0 --backend-server-url http://127.0.0.1:22500 &
curl -X PUT -H "content-type: application/json" --data '{"endpoint":"127.0.0.1:22400","gpu_group":"gpu-0"}' http://127.0.0.1:8007/internal/workers/cpp-worker-1
# 访问 https://127.0.0.1:8006/(playwright 录像:见 benchmark/demo-video,输入框 placeholder 'Type or press Space to record',fill(force)+Enter)
```

## 五、账号 / 访问点(凭证在各处 env,不写明文)

- **GitHub**:`hujinnvshi/minicpm-ascend-challenge`(HTTPS/SSH,git 凭证在用户 `~/.gitconfig`/ssh key)。**4 分支全 push**(main / p1.7 / g2-g3-admission / p2-duplex-exit0),main=`0ff3e83`=完整提交。
- **ModelScope**(数据同步通道):用户账号;大文件(权重/Daily-Omni Videos.tar)走此。`modelscope` pip 包已装在 venv。seed-tts-eval 数据 = 公开 `CowboyZ/seed-tts-eval`(只读)。
- **910B 云机**:HiDevLab,用户平台账号;**无公网入站**;出站仅 github/pypi/modelscope 通(**YouTube/HF 被封**)。
- **赛事官方文档(飞书)**:评测规范 `https://modelbest.feishu.cn/docx/U41vdXMmQo7tv3xW2p9c9uEanKe`(已摘录到 `docs/eval-spec.md`)。
- **同步通道**:本地↔910B = Git(decisions.md);大文件=ModelScope。

## 六、下一步优先级(评审后定,见上次复盘)

1. **【去 G2 最大风险,可现在做】TTS-Seed 小样本(20 条)提早证精度**:用 **vllm-omni 的 TTS system prompt**(`benchmark/seed-tts-eval/eval_ref/seed_tts_dataset.py` 里的 `SEED_TTS_DEFAULT_OMNI_SYSTEM_PROMPT`)+ 我们 llama-omni-server 生成 → 算 ASV(WavLM 本地)+ WER(需 whisper-large-v3)。不等官方脚本,先证 F16≈基线。
2. **8 项 Demo 检查正式跑 + 留证**(补 G3 准入证据)。
3. **量化"修调用"复核 + msprof**(低成本未探杠杆:Q8_0 为何不快?per-token 23ms 归因)。
4. **【等官方】** llama.cpp-omni benchmark 脚本 → 跑 TTS-Seed/Daily-Omni/VideoMME 正式精度数。
5. **【按余力】** T2W+LLM 定向并发(与 LLM+LLM 不同,未单独测)。

## 七、待办/阻塞(等外部)

- **官方 llama.cpp-omni benchmark 评测脚本**(赛事方未提供)—— 卡 3 项精度数。
- **whisper-large-v3 来源**(HF 封;待 ModelScope 镜像或官方)。
- **Daily-Omni Videos.tar**(用户手动下载 + ModelScope 同步)。
- **Video-MME 视频**(平台 `._____temp/videos_chunked_*.zip` 下载中,未完成)。

## 八、分支说明

- `main`(0ff3e83)= **完整提交**(P1.7 + 报告 + 复现 + 视频 + 规范 + eval 脚本)。
- `p2-duplex-exit0`= P2 性能诊断(并发/量化/exit-0 证伪,信息性,**未 merge main**)。
- `p1.7` / `g2-g3-admission` = 已 merge main 的 feature 分支(留作历史)。

## 九、环境约束(避坑)

- CANN **不支持 Q4_K_M** → 用 F16;**Q8_0 实测不提速**(dequant-bound,量化暂死刑,待"修调用"复核)。
- 910B **单 compute NPU**(npu-smi Total Count=1;dev_count=2 是聚合假象),**并发 runtime-capped 1.24×**(LLM+LLM)。
- 图模式 USE_ACL_GRAPH 910B **不支持**(头文件缺)。
- perf-duplex 的 exit 0/2/3 是**本工具双工实时门槛,非官方排名指标**;官方只看 **SPEAK→WAV RTF**。
