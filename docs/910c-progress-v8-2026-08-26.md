# 910C 实测与 v8 优化进展（2026-08-26 晚回写，v8 精度已验证版）

> 来源：gitcode/github 910c 分支提交 c7ec345「v8(910c): 集成 910C 图模式优化并补齐提交物」（910C 环境 agent 回传）
> 状态：**v8 精度已验证通过 → 提交门禁全过，v8 可打包提交官方**（替换在册 v5 0.8357，出分预估 ~0.73）

## 一、910C 环境与 v7 验证（全链路跑通）

- 环境：CANN Lab NPU A3 = **Ascend910 双 die 64GB**，CANN **9.1.0**（/home/developer/Ascend/cann-9.1.0），40 核单 NUMA，ccec 构建
- v7 代码（git 892d4c0）构建成功（3 目标）；构建坑 3 个：acl.h 路径（tools/omni/CMakeLists 硬编码 /usr/local/Ascend → 910C 本地改 CANN_INSTALL_DIR）、OpenSSL<3.0（-DLLAMA_OPENSSL=OFF）、ascendcl 链接（link.txt 注入）
- **v7 普通配置实测 RTF：0.8842/0.8836**（FA 生效 ✓、batch_validity 四字段全 true ✓）
- 对照：本机 910B2 v7 = 0.94；doma 910C = 0.54-0.69；**910C 普通版 0.88**

## 二、v8 优化实测（910C，假设驱动）

| 假设 | 结果 | 决策 |
|---|---|---|
| H1 图模式（GGML_CANN_ACL_GRAPH=1） | **-16%**（0.7345/0.7339，覆盖 89% compute 243/273，recapture 34 次） | ✅ v8 开启 |
| H2 线程 24→16 | **-2.0%**（0.8656 vs 0.8834；t32 超订 +1.5%） | ✅ v8 采纳（HEADCODE/T2W=16） |
| **组合 graph+t16** | **0.726（-17.9%）**（全 4 视频官方环 0.7233/0.729） | ✅ v8 最优配置 |
| fusion（ADD+RMS_NORM） | 0.8661/0.8704 噪声内 | ❌ |
| H3 worker 拓扑（1 vs 2） | 0.8647 vs 0.8656 噪声内 | ❌ 保持 2 worker |
| H5 t2w 步数 <5 | steps=3/4 → wav_events=0（静默失败） | ❌ 评测配置禁止 |
| H4 KV Q8_0 量化 | CANN 无 QUANTIZE 算子 + FA 只收 F16，收益折减 | ⏸ Phase 2（待 CANN FA 支持确认） |

**CANN FA contiguity 缺陷（910C 定位并修复，已入提交物 aclnn_ops.cpp）**：
- 现象：perf-duplex 主 LLM 多 token prefill 崩溃（FusedInferAttentionScore: key must be contiguous）
- 根因：ggml_cann_flash_attn_ext 的 transpose12 假设 K/V/Q head-major [D,N,S]；多 token prefill 是 sequence-major [D,S,N]
- 修复：aclnn_ops.cpp FA 入口 `make_bsnd_contiguous`——非规范 strides 检测 + 连续拷贝（B≤1 忽略 dim3）；perf-duplex 复活、rts 无回归
- 影响：解锁 perf-duplex + prefill 图模式 + 防评测边界崩溃（健壮性必修）

## 三、✅ v8 精度验证（910C，2026-08-26 晚完成——提交门禁全过）

| 验证项 | 结果 | 判定 |
|---|---|---|
| LLM 输出（llm_token_ids / llm_text） | 图模式 vs 普通 **逐字节一致** | ✅ |
| tts seed-zh 40 条 | WER **0.947% = 0.947%**（图=普通） | ✅ |
| tts wav 波形 | **40/40 逐字节一致** | ✅ |
| batch_validity | 四字段全 True（data_valid/realtime_eligible/core_sufficient/score_eligible） | ✅ |
| 唯一差异 | rts omni_duplex1 turn3 尾帧 flush 分段（文本相同、不入 core 帧，边界现象） | 不影响 |

**结论：图模式对精度零影响，v8 可提交。**

## 四、v8 最终 RTF（910C 官方环 5 轮）

| 项 | 值 |
|---|---|
| core RTF ×5 轮 | **0.7231 / 0.7240 / 0.7247 / 0.7228 / 0.7239**（均值 **0.7237**，散差 <0.3%） |
| 段分解（一轮） | encode **0.183** + prefill 0.012 + decode 0.169 + tts **0.121** + token2wav **0.238** |
| SPEAK→wav 均值 | 967.6 ms |
| 对照：v7 普通（910C 同环） | 0.8842/0.8836（encode 0.181 + prefill 0.012 + decode 0.173 + tts 0.218 + t2w 0.300） |
| 图模式收益分布 | **tts -39%（0.218→0.121）、t2w -20%（0.300→0.238）**（小算子密集段整图下发受益） |

**910C 段耗时新认知：token2wav 0.238 已是最大段**（CPU vocoder 不随 NPU 算力加速）→
Phase 2 第一候选 = t2w 步数修复（doma 4 步 flow 思路，预期 ~20%）。

## 五、v8 提交物清单（已随 c7ec345 就绪）

- **代码 6 文件**（官方树 + 补丁）：`ggml-cann.h`（per-backend 图模式接口，新增）+ `ggml-cann.cpp`（patch7 + 图模式实现）+ **`aclnn_ops.cpp`（FA contiguity 修复，新增）** + `omni.cpp` + `omni.h` + `vision.cpp`（图模式受限使能）
- **env 表（README §2.1，910C 提交值）**：`GGML_CANN_ACL_GRAPH=1` + `OMNI_HEADCODE_THREADS=16` + `OMNI_T2W_THREADS=16` + v7 保留（FORCE_FA/VISION_FA/NPU_SERIAL/VISION_BATCH_ALL 默认开/NZ=off）
- **README v8 化**：目标硬件 910C；构建补 `-DUSE_ACL_GRAPH=ON`；RTF 5 轮 + 精度逐字节验证记录（§5.2）
- **打包脚本**：4 文件 → **6 文件**（+ggml-cann.h +aclnn_ops.cpp）；`OFFICIAL_TMP` env 可配置官方仓库路径
- **⚠️ 排除**：tools/omni/CMakeLists.txt 的 910C 本地改动（ascendcl 直链/CANN_INSTALL_DIR 适配）——官方不可改文件清单，提交=校验失败

## 六、风险与下一步

- v8 提交时机：**精度门禁已过，可打包提交**（每天限 3 次，8/31 截止；v8 会覆盖在册 v5 0.8357，出分预估 ~0.73 → 排名 6-8 名区间）
- 打包动作：本机 `scripts/package-submission.sh`（6 文件）→ 提交官方（README 哈希替换）
- Phase 2 候选（910C 继续，按杠杆排序）：
  1. **t2w 步数修复**（~20%，910C 最大段）——doma 4 步 flow 思路（env 读取 + prompt_cache 匹配）；910B4 曾 CLOSED 5 步拐点，910C 重试
  2. KV 量化 INT8（decode ~20%）——先探针 CANN FA 是否收非 F16 KV
  3. FP8 权重（-4.6% 预估）
  4. 大 ctx 扫描（16384/32768，910C 64GB）
  5. 量化档重扫（CANN 六档 + Q8_0 质量检查，CANN 无 CUDA 重复 token bug 可能复活）
- 完整项目优化提示词：docs/910c-test-prompt-2026-08-26.md（v2，SSH clone + 精度门禁 + 假设驱动纪律）
