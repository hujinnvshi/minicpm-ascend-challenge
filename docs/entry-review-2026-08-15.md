# 赛事准入评审报告（2026-08-15 · review-optimize）

> 评审对象：赛道一·子赛道 A（llama.cpp-omni）当前准备状态
> 评审口径：官方《评测规范说明》5 步流程 + submission-checklist.md，逐项对照证据与 NZ 配置状态
> 结论先行：**精度准入不满足（Video-MME 硬缺口 + TTS 口径待复核）；性能满足与否取决于 NZ 口径（风险大）；Demo 待新机器复验；复现基本就绪但数字口径需统一。**

---

## 一、逐项评审

### 1. 框架与环境检查 ✅
| 项 | 证据 | 判定 |
|---|---|---|
| 指定框架 llama.cpp-omni | 完整源码（6 CANN 补丁 + P1.7 队列 + 协议门控），全量重编 3 次成功（ccec, GGML_CANN=ON）| ✅ |
| 官方昇腾环境可部署 | eval-singlecard-910b.env（单卡适配, 锁 die0, NUM_GPUS=1）+ build-cann.sh + 权重预置 | ✅ |

### 2. 精度准入（全部满足才进性能排名）❌
| Benchmark | 基线 | 准入 | 我方 | NZ 状态 | 判定 |
|---|---|---|---|---|---|
| Daily-Omni | 79.5 | ≥77.5 | **79.8%**（全量 1196, 官方 run_all 路径）| NZ=off ✓ | ✅ 达标（+0.3pp 噪声内, 降幅规则安全）|
| TTS-Seed WER | 1.414 | ≤1.56 | **1.501%**（全量 2020）| ⚠️ **NZ=on 生成**（run_tts_eval_cpp_zh.sh 无 NZ export；官方 run_eval.py tts 任务注入 off）| ⚠️ 数字有效性待复核, 余量仅 0.059 |
| TTS-Seed ASV | 0.709 | ≥0.689 | **0.694** | ⚠️ 同上 | ⚠️ 余量仅 0.005, 最薄 |
| **Video-MME** | 69.0 | ≥67.0 | 51.5-53.5%（99q KB）/ **63.3%±5.7pp**（270 题合池, NZ=off 干净）| NZ=off ✓ | ❌ **点估计 63.3 < 67**（区间上沿 69 擦线）→ 精度准入不满足 → 按规则不能进入性能排名 |

> Video-MME 现状详析：99q KB 域 51.5-53.5%（KB 视频异质性极大 78/44/52）；270 题合池（KB+非KB 分层）63.3%±5.7pp；全量 2700 未测。**即使全量落在区间上沿（~69）也仅擦线**。唯一翻盘路径 = 基线口径申诉（见 §三）。

### 3. Demo 可用性验证 ⚠️
| 要求 | 证据 | 判定 |
|---|---|---|
| 接入官方 Demo 端到端/流式/稳定 | 08-06 8 项检查全过（**旧机器**）+ demo_turnchat.webm + demo-evidence/ | ⚠️ **新机器（08-14 迁移后）未复验**，当前端口无监听。需重新起 3 进程验证（~10min）|

### 4. 性能评测（排名核心）⚠️⚠️
| 要求 | 我方 | NZ 状态 | 判定 |
|---|---|---|---|
| SPEAK→WAV RTF < 1.087（越低越好）| **0.58-0.59**（24 线程+NUMA, NZ=on 直跑测得）| ⚠️ **官方 run_eval.py RTS 任务注入 NZ=off**（L321）；NZ=off 下并行会话实测 **e2e 1.08**（对照不对称, 待独占确认）| ⚠️ 若官方 RTF 口径 = NZ=off → **优势归零（1.08 ≈ 基线 1.087 擦线）**；若官方不强制（FAQ 的 off 论证仅针对精度任务）→ 0.58 成立但需披露 |

### 5. 工程复现审查 ⚠️
| 要求 | 证据 | 判定 |
|---|---|---|
| 提交代码/配置/脚本/视频/复现文档可重跑 | 打包流程重写 + dist/submission_v1-final_20260815.tar.gz（41M, 3562 文件, MANIFEST）| ⚠️ **包内 TTS/RTF 数字为 NZ=on 数据**, 评审按文档（NZ=off 官方路径）重跑会得不同数字 → "结果明显不一致"风险; 需按 NZ 口径统一后重打 |

---

## 二、综合判定

| 准入项 | 状态 | 缺口 |
|---|---|---|
| 框架/环境 | ✅ | — |
| 精度准入 | ❌ | Video-MME 硬缺口（63.3 < 67）; TTS 口径待复核 |
| Demo | ⚠️ | 新机器未复验 |
| 性能 | ⚠️ | **2026-08-15 定论**：独占 A/B NZ=on e2e 1.01（beat ~7%）/ NZ=off 1.08（擦线）——取决于官方 rts 任务 NZ 口径（Q5 待赛方确认）|
| 复现 | ⚠️ | 数字口径统一 + 重打包 |

**结论：当前不满足全部赛事入门要求。** 排序：Video-MME（硬缺口, 唯一出路=基线申诉）> TTS 口径（可复核修复）> RTF×NZ（口径决定）> Demo（可快速复验）。

---

## 三、行动优先级（距提交截止 08-31 剩 16 天）

**P0（决定生死）**
1. **RTF×NZ 独占 A/B**（并行会话结束后, ~15min）：NZ=on vs off 各 ≥2 次（同条件 24 线程+NUMA）。确定性能是否满足 → 决定性能报告口径与申诉内容。
2. **Video-MME 基线口径申诉发出**（更新 organizer-inquiry-2026-08-12.md, 补 63.3%±5.7 新证据 + 剔除空响应论证）：唯一翻盘路径。核心论点 = 69.0 疑似 vLLM/910C 口径, 子赛道 A 在 910B + 官方 evaluation/ 的独立基线 ≈63%±6。

**P1（可修复）**
3. **TTS NZ=off 复核**：重建 venv-tts（1-2h, 坑见 tts-seed-eval.md）+ 子集 200 条（~40min）。WER/ASV 若达标 → 数字转正; 若不达标 → 需找 NZ=off 下的生成优化或申诉。
4. **Demo 新机器复验**（~10min）：3 进程 + 文本/流式 + /health, 更新 demo-evidence。

**P2（收尾）**
5. 提交物数字按 NZ 口径统一（performance-report/reproduce-guide/competition-readiness 标注 RTF 与 TTS 的 NZ 状态）+ 重新打包 v1-final。

**纪律**：所有新评测必须官方路径或显式 export GGML_CANN_WEIGHT_NZ=off; 所有新数字记录 NZ 状态。
